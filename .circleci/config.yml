version: 2.1
jobs:
  build:
    docker:
      - image: circleci/python:3.6.4
    steps:
      - checkout
      - run:
          name: pyenv
          command: |
            suddo apt-get update
            sudo apt-get install -y make build-essential libssl-dev zlib1g-dev \
            libbz2-dev libreadline-dev libsqlite3-dev wget curl llvm \
            libncursesw5-dev xz-utils tk-dev libxml2-dev linxmlsec1-dev libffi-dev liblzma-dev
            sudo pip3 install nox==2020.8.22

            git clone https://github.com/pyenv/pyenv.git ~/.pyenv
            cd ~/.pyenv && src/configure && make -C src
            cd
            echo 'export PATH=$HOME/.pyenv/bin:$PATH' >> $BASH_ENV
            echo 'export python38=3.8.12' >> $BASH_ENV
            source $BASH_ENV
            eval "$(pyenv init --path)"
            eval "$(pyenv init -)"
            pyenv install $python38
      - run:
          name:
          command: |
            source $BASH_ENV
            eval "$(pyenv init --path)"
            eval "$(pyenv init -)"
            nox
      - persist_to_workspace:
          root: /tmp
          paths:
            - .


  test:
    docker:
      - image: circleci/python:3.6.4
    steps:
      - checkout
      - attach_workspace:
          at: /tmp
      - run:
          name:
          command: |
            source $BASH_ENV
            eval "$(pyenv init --path)"
            eval "$(pyenv init -)"
            nox


workflows:
  version: 2
  commit_workflow:
    jobs:
      - build