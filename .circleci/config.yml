version: 2.1
jobs:
  build:
    working_directory: ~/project
    docker:
      - image: circleci/python:3.6.4
    steps:
      - checkout
      - run: pip install nox=2021.6.12
      - run: nox
workflows:
  version: 2
  commit_workflow:
    jobs:
      - build